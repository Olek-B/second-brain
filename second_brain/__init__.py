"""Second Brain - AI-driven markdown knowledge base with graph visualization."""

import logging

__version__ = "0.1.0"

# Configure root logger for the application
logging.getLogger("second_brain").addHandler(logging.NullHandler())
